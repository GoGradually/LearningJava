spring:
  datasource:
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
    show-sql: true
logging.level:
  org:
    hibernate:
      SQL: debug
      type.descriptor.sql: trace